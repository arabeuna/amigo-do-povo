# Variáveis de ambiente para o Render
# Configure estas variáveis no painel do Render em: Environment → Environment Variables

# Configurações do Banco de Dados PostgreSQL (Supabase)
DB_HOST=db.sfmupywgitezmkdsmvxt.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=DHWo17JPNKmYcv9p

# Configurações do JWT
JWT_SECRET=your-secret-key
JWT_EXPIRES_IN=24h

# Configurações do Servidor
PORT=10000
NODE_ENV=production

# Configurações de CORS
CORS_ORIGIN=https://seu-app.onrender.com

# Configurações de Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000 